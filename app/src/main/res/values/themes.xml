<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme: Compose handles theming. This is just the window background. -->
    <style name="Theme.EdgeTele" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
